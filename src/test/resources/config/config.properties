# Default local framework configuration.
#
# Values in this file can be overridden at runtime with Maven system
# properties, for example:
# mvn test -Dbrowser=chrome -Dheadless=false

browser=chrome
headless=true
baseUrl=https://www.saucedemo.com/
explicitWaitSeconds=10
pageLoadTimeoutSeconds=30
implicitWaitSeconds=0
windowWidth=1440
windowHeight=900
retryCount=0
